---
phase: 04-distribution
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - docs/screenshot.png
  - docs/demo.gif
autonomous: false
requirements:
  - DIST-01
  - DIST-02

user_setup:
  - service: azure-trusted-signing
    why: "OIDC Workload Identity Federation must be configured for the NYBaywatch/claude-from-here repo to allow tag-triggered signing"
    dashboard_config:
      - task: "Verify or create federated credential with subject repo:NYBaywatch/claude-from-here:ref:refs/tags/v*"
        location: "Azure Portal -> App Registrations -> [service principal] -> Certificates & secrets -> Federated credentials"
      - task: "Verify service principal has Artifact Signing Certificate Profile Signer role on the certificate profile"
        location: "Azure Portal -> Trusted Signing Account -> Certificate Profiles -> Access Control (IAM)"
  - service: github-repo
    why: "Repository secrets needed for Azure signing in CI"
    dashboard_config:
      - task: "Add repository secrets: AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID, AZURE_SIGNING_ACCOUNT, AZURE_SIGNING_PROFILE"
        location: "GitHub -> NYBaywatch/claude-from-here -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "docs/ folder contains screenshot.png and demo.gif media assets per D-04"
    - "Screenshot shows the Windows 11 context menu with 'Claude from here' item visible"
    - "GIF shows the full interaction: right-click -> click -> Terminal opens with Claude"
    - "Repo is clean for public consumption — no .planning/, .claude/, scripts/, .reg files per D-10"
    - "GitHub Release v1.0.0 exists with downloadable ClaudeFromHere-Setup.exe per D-05, D-07"
  artifacts:
    - path: "docs/screenshot.png"
      provides: "Static context menu screenshot"
    - path: "docs/demo.gif"
      provides: "Animated demo of full interaction"
  key_links:
    - from: "README.md"
      to: "docs/screenshot.png"
      via: "markdown image reference"
      pattern: "docs/screenshot.png"
    - from: "README.md"
      to: "docs/demo.gif"
      via: "markdown image reference"
      pattern: "docs/demo.gif"
---

<objective>
Capture media assets, clean the repo for public consumption, and prepare for the v1.0.0 tag push that triggers the CI release pipeline.

Purpose: The README references screenshot and GIF assets (D-04). The repo must be cleaned of dev scaffolding before going public (D-10). The v1.0.0 tag triggers the release workflow created in plan 02.
Output: docs/screenshot.png, docs/demo.gif, cleaned repo ready for public release
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution/04-CONTEXT.md
@.planning/phases/04-distribution/04-RESEARCH.md
@.planning/phases/04-distribution/04-01-SUMMARY.md
@.planning/phases/04-distribution/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ directory and repo cleanup script</name>
  <files>docs/.gitkeep</files>
  <read_first>
    - D:\Working\Projects\claude-from-here\.planning\phases\04-distribution\04-CONTEXT.md (D-10 cleanup list)
  </read_first>
  <action>
Create the `docs/` directory with a `.gitkeep` placeholder file so the directory is tracked by git. The actual screenshot.png and demo.gif will be added by the user in the next task (checkpoint).

Also prepare for the repo cleanup (D-10) by listing exactly what will be removed. The cleanup itself must be done as a dedicated commit AFTER media assets are captured and committed, and BEFORE tagging v1.0.0 (per Pitfall 5 from research).

Files/directories to remove in the cleanup commit:
- `.planning/` (entire directory)
- `.claude/` (entire directory)
- `GETTING-STARTED.md`
- `claude-from-here.reg`
- `claude-from-here-uninstall.reg`
- `scripts/` (entire directory — dev registration scripts)

Do NOT remove:
- `build-installer.ps1` (valuable for contributors per research recommendation)
- `CMakeLists.txt`, `src/`, `package/`, `assets/`, `installer/`
- `README.md`, `LICENSE`, `.github/`, `.gitignore`, `CLAUDE.md`

Note: The actual `git rm` cleanup and `git tag v1.0.0` commands will be run by the user after verifying everything. This task only creates the docs directory.
  </action>
  <verify>
    <automated>test -d docs && echo "docs/ directory exists"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/` directory exists in the repo
    - `docs/.gitkeep` file exists
  </acceptance_criteria>
  <done>docs/ directory created and tracked by git, ready to receive media assets</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture media assets, clean repo, and release</name>
  <files>docs/screenshot.png, docs/demo.gif</files>
  <action>
This is a human checkpoint. The executor pauses and presents the verification steps below to the user. The user captures the media, cleans the repo, configures Azure OIDC, sets GitHub secrets, and pushes the v1.0.0 tag.
  </action>
  <what-built>
The docs/ directory is ready. The README (from plan 01) references `docs/screenshot.png` and `docs/demo.gif`. These must be captured on the developer's machine since they require an interactive Windows 11 desktop with the extension installed and working.
  </what-built>
  <how-to-verify>
**Screenshot (docs/screenshot.png):**
1. Right-click a folder in File Explorer
2. Capture a screenshot showing the modern context menu with "Claude from here" and the Claude icon visible
3. Save as `docs/screenshot.png` (PNG format, crop to just the context menu area)
4. Use Windows Snipping Tool (Win+Shift+S) or ShareX

**Animated GIF (docs/demo.gif):**
1. Open ScreenToGif (download from screentogif.com if needed)
2. Record: right-click folder -> click "Claude from here" -> Windows Terminal opens with Claude running
3. Edit frames if needed (trim, crop)
4. Export as `docs/demo.gif`
5. Keep file size reasonable (under 5MB)

**After capturing both files:**
```
git add docs/screenshot.png docs/demo.gif
git commit -m "docs: add screenshot and demo GIF for README"
```

**Verify the README renders correctly:**
- Open README.md in a markdown preview
- Confirm both images display inline

**Then proceed with repo cleanup (per D-10):**
```powershell
git rm -r .planning/
git rm -r .claude/
git rm GETTING-STARTED.md
git rm claude-from-here.reg
git rm claude-from-here-uninstall.reg
git rm -r scripts/
git commit -m "chore: remove dev scaffolding for public release"
```

**Verify Azure OIDC setup (required for CI signing):**
- Confirm federated credential subject: `repo:NYBaywatch/claude-from-here:ref:refs/tags/v*`
- Confirm GitHub repo secrets are set: AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID, AZURE_SIGNING_ACCOUNT, AZURE_SIGNING_PROFILE

**Push and tag (per D-07):**
```
git push origin master
git tag v1.0.0
git push origin v1.0.0
```

This will trigger the GitHub Actions release workflow.
  </how-to-verify>
  <verify>
    <automated>test -f docs/screenshot.png && test -f docs/demo.gif && echo "Media assets present"</automated>
  </verify>
  <acceptance_criteria>
    - docs/screenshot.png exists and is a valid PNG file
    - docs/demo.gif exists and is a valid GIF file
    - .planning/ directory no longer tracked in git
    - .claude/ directory no longer tracked in git
    - scripts/ directory no longer tracked in git
    - GETTING-STARTED.md no longer tracked in git
    - claude-from-here.reg and claude-from-here-uninstall.reg no longer tracked in git
    - v1.0.0 tag exists on the repository
    - GitHub Actions release workflow completed successfully
  </acceptance_criteria>
  <done>Media assets captured and committed, repo cleaned for public release, v1.0.0 tagged and pushed, GitHub Release created with ClaudeFromHere-Setup.exe</done>
  <resume-signal>Type "released" after the v1.0.0 tag has been pushed and the GitHub Actions workflow has completed successfully, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- docs/screenshot.png exists and shows the context menu
- docs/demo.gif exists and shows the full interaction
- Dev scaffolding removed (.planning/, .claude/, scripts/, .reg files, GETTING-STARTED.md)
- v1.0.0 tag pushed and CI workflow completed
- GitHub Release created with ClaudeFromHere-Setup.exe downloadable
</verification>

<success_criteria>
- Media assets committed to docs/ and referenced correctly from README
- Repository cleaned of all dev scaffolding per D-10
- v1.0.0 GitHub Release exists at github.com/NYBaywatch/claude-from-here/releases with ClaudeFromHere-Setup.exe attached
- A user visiting the repo can read the README, see the screenshots, and download the installer
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-03-SUMMARY.md`
</output>
